PORT=8000  //this backend port
DB_USER=postgres //postgress name of the user
DB_PASSWORD=postgres  // password of the postgress
DB_NAME=taskmanager  // database name
DB_PORT=5432  // where the datbas run
JWT_SECRET=secret123  // secret key whicch is used to create the token for the authentication and authorzation

#server fil
## server is the main file which run backend code all th file include in th serer file


### package.json=> yo install the packages which package whcih are use in your application
### package-lock.json=>this store the informaion about the package which type fo the information this depencieds


### config foldr db.js to conected db to the backend or my application



## env =>environment varaile any time stmp maail server they crenetdaitls host nm port 
## port =9090,8080,2000,5000, where the serer is ruuning

### utils folders we put that file whic are every time

### routes folder api folder

### api >used for the data transfer from the server and frontend
#### 
